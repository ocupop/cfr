import React from 'react'
import PropTypes from 'prop-types'

const ApparelSlider = ({ collection, children }) => {
  return (
    <div>
      TODO: Add image slider here.
      {children}
    </div>
  )
}

ApparelSlider.propTypes = {
  collection: PropTypes.string
}

export default ApparelSlider
